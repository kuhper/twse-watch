services:
  - type: web
    name: twse-watch
    runtime: python
    plan: free
    startCommand: uvicorn app7:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
